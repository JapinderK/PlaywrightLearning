import { BASE_URL, firstTest } from "../testutils.js";

//{} are used when there is no default function is available in the exported file.

console.log(BASE_URL);

let result = firstTest("Login Page");
console.log(result);