enum testStatus{
    Pass="Pass",
    fail="Fail",
    skip="Skip"
}

console.log(testStatus.Pass);

//Enums stores only static data like test status or environment values - staging, dev
//they are used for constant values